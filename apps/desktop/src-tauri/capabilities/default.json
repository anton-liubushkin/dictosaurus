{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the settings and overlay windows",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "macos-permissions:default",
    "autostart:default"
  ]
}
